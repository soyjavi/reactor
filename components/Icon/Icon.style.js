import { StyleSheet } from 'react-native';

import { THEME } from '../../common';

const { UNIT } = THEME;

export default StyleSheet.create({
  container: {
    height: UNIT * 2,
    width: UNIT * 2,
  },
});
